{
  "version": 2,
  "builds": [
    {
      "src": "build/index.js",
      "use": "@now/static",
      "config": { "distDir": "build" }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "build/$1" }]
}
