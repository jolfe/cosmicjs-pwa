{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@now/static",
      "config": { "distDir": "build" }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "build/$1" }]
}
